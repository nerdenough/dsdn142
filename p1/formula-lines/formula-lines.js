/**
 * Brendan Goodenough
 * Project 1 - Formula Lines
 *
 * Based on the Project 1 "Lines Formula" template by Birgit Bachler
 */

function setup() {
  // Injects the canvas into a specific container
  const canvas = createCanvas(600, 600);
  canvas.parent('canvas');
  smooth();
}

function draw() {
  clear();
  background(255);
}
